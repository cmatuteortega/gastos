<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- No pedimos INTERNET a propósito: todo el procesado es 100% local -->

    <application
        android:allowBackup="false"
        android:label="Gastos Listener"
        android:theme="@style/Theme.Material3.DayNight.NoActionBar">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            El sistema exige declarar el servicio así, con el permiso
            BIND_NOTIFICATION_LISTENER_SERVICE. El usuario tiene que activarlo
            a mano en Ajustes > Apps > Acceso especial > Acceso a notificaciones.
        -->
        <service
            android:name=".NotificationListener"
            android:exported="false"
            android:label="Gastos Listener"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

    </application>
</manifest>
